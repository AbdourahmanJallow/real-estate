export enum TransactionStatus {
  PENDING = 'pending',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}

export enum TransactionType {
  PURCHASE = 'purchase',
  RENTAL = 'rental',
  //   CANCELLED = 'cancelled',
}
